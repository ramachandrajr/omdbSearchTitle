
// May be we should do on submit.
$("form").on("submit",function () {
	// If the input is empty.
	if ($("#main-search").val().length === 0) {
		// Can't submit the form.
		return false;		
	}
	// The input is not empty.
	else {

	}
});

